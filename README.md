# PDF Pair

An offline-first, open-source PDF comparison reader for Windows and Ubuntu.

## Development

```bash
npm install
npm test
npm run typecheck
npm run dev
```

The desktop shell is built with Tauri, while the reader UI uses React and TypeScript.
